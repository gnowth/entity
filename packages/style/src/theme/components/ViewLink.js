import { css } from 'styled-components';

import { color } from '../../selectors';

// eslint-disable-next-line import/prefer-default-export
export const component_viewLink_standard = css`
  color: ${color({ name: 'black' })};
`;
