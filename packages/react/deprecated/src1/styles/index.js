import vars from 'styles/vars';
import mixins from 'styles/mixins';
import typesets from 'styles/lib-typesets';
import widgets from 'styles/lib-widgets';
import ui from 'styles/lib-ui';

export { default as global } from 'styles/global';

export default {
  lib: {
    ui,
    widgets,
    typesets,
  },
  vars,
  mixins,
};
