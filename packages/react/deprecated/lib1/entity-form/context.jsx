import React from 'react';

const Context = React.createContext();

export const { Provider, Consumer } = Context;

export const ContextInput = () => (<div />);

export const ContextForm = () => (<div />);
